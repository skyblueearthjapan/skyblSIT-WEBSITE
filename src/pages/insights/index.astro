---
// Insights Page - ナレッジ・ブログ一覧ページ
// 将来的にはContent Collectionsを使って記事を管理

import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const pageTitle = 'ナレッジ';
const pageDescription = '中小企業DX・AI活用に関する知見・ノウハウを発信。現場視点で役立つ情報をお届けします。';

// プレースホルダー記事（将来的にはContent Collectionsから取得）
const articles = [
  {
    slug: 'ai-search-trap',
    title: '中小企業こそ生成AIを「検索」として使ってはいけない',
    description: 'ChatGPTを単なる検索エンジンの代わりとして使うと失敗します。業務フローに組み込むための具体的なプロンプト設計思考を解説。',
    category: 'AI活用',
    pubDate: '2024-03-20',
    image: null,
  },
  {
    slug: 'appsheet-first-app',
    title: 'AppSheetで最初のアプリを作る前に知るべき3つのこと',
    description: 'ノーコードだからといって闇雲に作り始めると失敗します。業務アプリ開発で最初に考えるべきポイントを解説。',
    category: 'アプリ開発',
    pubDate: '2024-03-15',
    image: null,
  },
  {
    slug: 'dx-without-destruction',
    title: '「壊さないDX」とは何か？中小企業が陥る変革の落とし穴',
    description: '大企業の成功事例をそのまま中小企業に当てはめると失敗します。現場を味方につけるDXの進め方。',
    category: 'DX推進',
    pubDate: '2024-03-10',
    image: null,
  },
  {
    slug: 'spreadsheet-to-app',
    title: 'スプレッドシートを"アプリ化"するメリットと注意点',
    description: 'Excelやスプレッドシートで管理している業務をアプリにするべきか？判断基準と移行のコツを解説。',
    category: 'アプリ開発',
    pubDate: '2024-03-05',
    image: null,
  },
];

const categories = ['すべて', 'AI活用', 'DX推進', 'アプリ開発', 'データ活用', '経営マインド'];
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <Header slot="header" currentPath="/insights" />
  
  <!-- Page Hero -->
  <section class="page-hero">
    <div class="container">
      <span class="badge">INSIGHTS</span>
      <h1>ナレッジ</h1>
      <p class="page-subtitle">
        中小企業DX・AI活用に関する知見・ノウハウを発信します。<br>
        現場視点で役立つ情報をお届けします。
      </p>
    </div>
  </section>
  
  <!-- Articles Section -->
  <section class="section articles-section">
    <div class="container">
      <!-- Category Filter -->
      <div class="category-filter">
        {categories.map((cat, index) => (
          <button class:list={['filter-btn', { active: index === 0 }]}>
            {cat}
          </button>
        ))}
      </div>
      
      <!-- Articles Grid -->
      <div class="articles-grid stagger">
        {articles.map(article => (
          <article class="article-card card">
            <div class="article-image">
              {article.image ? (
                <img src={article.image} alt={article.title} />
              ) : (
                <div class="image-placeholder">
                  <span class="placeholder-icon">📝</span>
                </div>
              )}
            </div>
            <div class="article-content">
              <div class="article-meta">
                <span class="article-category">{article.category}</span>
                <time datetime={article.pubDate}>{article.pubDate}</time>
              </div>
              <h2 class="article-title">
                <a href={`/insights/${article.slug}`}>{article.title}</a>
              </h2>
              <p class="article-desc">{article.description}</p>
              <a href={`/insights/${article.slug}`} class="read-more">
                続きを読む
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M5 12h14M12 5l7 7-7 7"/>
                </svg>
              </a>
            </div>
          </article>
        ))}
      </div>
      
      <!-- Coming Soon Notice -->
      <div class="coming-soon card">
        <p>📚 記事は順次追加予定です。お楽しみに！</p>
      </div>
    </div>
  </section>
  
  <!-- Newsletter CTA -->
  <section class="section newsletter-section">
    <div class="container">
      <div class="newsletter-card card">
        <h2>最新情報をお届けします</h2>
        <p>DX・AI活用のヒントや最新記事をメールでお届けします。</p>
        <form class="newsletter-form">
          <input type="email" placeholder="メールアドレス" required>
          <button type="submit" class="btn btn-primary">登録する</button>
        </form>
      </div>
    </div>
  </section>
  
  <Footer slot="footer" />
</BaseLayout>

<style>
  /* Page Hero */
  .page-hero {
    padding: calc(80px + var(--space-3xl)) 0 var(--space-2xl);
    text-align: center;
    background: linear-gradient(to bottom, var(--bg-surface), var(--bg-base));
  }
  
  .page-hero .badge {
    margin-bottom: var(--space-md);
  }
  
  .page-hero h1 {
    margin-bottom: var(--space-md);
  }
  
  .page-subtitle {
    color: var(--text-secondary);
    font-size: 1.1rem;
  }
  
  /* Category Filter */
  .category-filter {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    justify-content: center;
    margin-bottom: var(--space-2xl);
  }
  
  .filter-btn {
    padding: var(--space-sm) var(--space-lg);
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-muted);
    background: transparent;
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-full);
    cursor: pointer;
    transition: all var(--transition-fast);
  }
  
  .filter-btn:hover {
    color: var(--text-primary);
    border-color: var(--color-primary);
  }
  
  .filter-btn.active {
    color: white;
    background: var(--color-primary);
    border-color: var(--color-primary);
  }
  
  /* Articles Grid */
  .articles-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--space-xl);
    margin-bottom: var(--space-2xl);
  }
  
  .article-card {
    display: flex;
    flex-direction: column;
    padding: 0;
    overflow: hidden;
  }
  
  .article-image {
    aspect-ratio: 16 / 9;
    overflow: hidden;
  }
  
  .article-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-base);
  }
  
  .article-card:hover .article-image img {
    transform: scale(1.05);
  }
  
  .image-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(16, 185, 129, 0.05));
  }
  
  .placeholder-icon {
    font-size: 3rem;
    opacity: 0.3;
  }
  
  .article-content {
    padding: var(--space-lg);
    display: flex;
    flex-direction: column;
    flex-grow: 1;
  }
  
  .article-meta {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-sm);
  }
  
  .article-category {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-primary-light);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .article-meta time {
    font-size: 0.8rem;
    color: var(--text-muted);
  }
  
  .article-title {
    font-size: 1.1rem;
    line-height: 1.4;
    margin-bottom: var(--space-sm);
  }
  
  .article-title a {
    color: var(--text-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }
  
  .article-title a:hover {
    color: var(--color-primary-light);
  }
  
  .article-desc {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.6;
    flex-grow: 1;
    margin-bottom: var(--space-md);
  }
  
  .read-more {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-primary-light);
    text-decoration: none;
    transition: gap var(--transition-fast);
  }
  
  .read-more:hover {
    gap: var(--space-sm);
  }
  
  /* Coming Soon */
  .coming-soon {
    text-align: center;
    padding: var(--space-xl);
    background: rgba(59, 130, 246, 0.05);
    border: 1px dashed var(--glass-border);
  }
  
  .coming-soon p {
    color: var(--text-muted);
    margin: 0;
  }
  
  /* Newsletter Section */
  .newsletter-section {
    background: var(--bg-surface);
  }
  
  .newsletter-card {
    text-align: center;
    padding: var(--space-3xl);
    max-width: 600px;
    margin-inline: auto;
  }
  
  .newsletter-card h2 {
    margin-bottom: var(--space-sm);
  }
  
  .newsletter-card p {
    color: var(--text-muted);
    margin-bottom: var(--space-xl);
  }
  
  .newsletter-form {
    display: flex;
    gap: var(--space-sm);
  }
  
  .newsletter-form input {
    flex: 1;
    padding: var(--space-md);
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--text-primary);
    background: var(--bg-base);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-md);
  }
  
  .newsletter-form input:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
  }
  
  @media (max-width: 480px) {
    .newsletter-form {
      flex-direction: column;
    }
    
    .newsletter-form .btn {
      width: 100%;
    }
  }
</style>
